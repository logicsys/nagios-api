define command {
    command_name    check-host-alive
    command_line    $USER1$/check_ping -H $HOSTADDRESS$ -w 3000.0,80% -c 5000.0,100% -p 1
}

define command {
    command_name    check_local_load
    command_line    $USER1$/check_load -w $ARG1$ -c $ARG2$
}

define command {
    command_name    check_local_disk
    command_line    $USER1$/check_disk -w $ARG1$ -c $ARG2$ -p $ARG3$
}

define command {
    command_name    check_ping
    command_line    $USER1$/check_ping -H $HOSTADDRESS$ -w $ARG1$ -c $ARG2$ -p 1
}

define command {
    command_name    return_ok
    command_line    /bin/echo "OK - Test service running"
}

define command {
    command_name    return_warning
    command_line    /bin/sh -c 'echo "WARNING - Test warning" && exit 1'
}

define command {
    command_name    return_critical
    command_line    /bin/sh -c 'echo "CRITICAL - Test critical" && exit 2'
}
